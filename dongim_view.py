# my_view.py


def render_schedule(styled_schedule):
    # Render the DataFrame (styled_schedule) in your desired way
    # For example, you can render it as a string
    print(styled_schedule.to_string(index=False))
